# Abhängigkeiten & Build-Ausgaben (werden generiert, gehören nicht ins Repo)
node_modules/
dist/

# Electron-Download-Cache (~138 MB)
electron.zip

# Gebaute Installer / Update-Artefakte (gehoeren auf den Webserver, nicht ins Repo)
# (Installer liegen nur in dist/ [schon ignoriert] und website/updates/)
website/updates/
*.blockmap

# Harness-/Editor-lokale Dateien
.claude/

# OS / Editor
Thumbs.db
Desktop.ini
*.log

# grosse Website-Demo nicht ins Repo
website/demo.mp4

# PawnIO-Testlab-Artefakte (Downloads/Referenzquellen, kein Projektcode)
_testlab/PawnIO_setup.exe
_testlab/pawnio-modules.zip
_testlab/pawnio-modules/
_testlab/pawnio-out.txt
_testlab/*.p
_testlab/PawnIOLib.h

# Gebuendelte Binaries (ffmpeg/mediamtx/Capture-Helfer) – teils >100 MB (GitHub-Limit).
# Auf einem neuen Rechner separat beziehen: ffmpeg (BtbN LGPL), mediamtx (Release),
# lumora-capture per `dotnet publish` aus capture/ bauen.
bin/
capture/bin/
capture/obj/

# TLS-Schluessel/Zertifikat fuer das lokale Streaming (Secrets – NIE ins Repo)
auto.key
auto.crt

# Deployment-Geheimnisse (Token) – NIE ins Repo. Nur die *.example-Vorlagen sind getrackt.
# Das Geheimnis-File hat einen unauffaelligen Zufallsnamen (Praefix lib-), damit es
# nicht erratbar ist; der Endpunkt selbst (sync-<random>.php) IST getrackt (Code).
website/lib-*.php
_testlab/deploy.local.json
_testlab/deploy-plan*.json
# signtool aus dem Windows-SDK-BuildTools-NuGet (~21 MB) - lokales Werkzeug, nicht ins Repo
_testlab/tools/

# Rohe Screenshot-Originale (nur lokal, optimierte .webp/.jpg sind versioniert)
/1.png
/2.png
website/stream-neu.png
website/stream-stats.png
